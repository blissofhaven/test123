# Передача Max: исправления по снимкам схемы

31.08.2026. Это результат визуальной корректировки, не новый этап карты.
Архив: `rza-2026-08-31-ui-schematic-fixes.zip`; SHA-256 лежит рядом в одноимённом
файле `.sha256`. Полный прогон **1476 passed, 0 failed, 0 skipped**;
native Windows целевой набор **139 passed**. Расхождений с эталоном нет.
Итоговые числа — в `status.json` и отчёте
`docs/work-verification/schematic-visual-fix-report.md`.

Исправлены нечитаемые состояния кнопок панели, длинные команды поворота,
ложные узлы пересечения и ложная красная рамка при допустимом движении шины.
Предпросмотр теперь использует те же правила столкновений, что и сохранение.
На физических линиях вместо двух
засечек стоят тонкие стрелки по смысловым портам from/to. Они обходят мостики
и разрывы и не закрываются центральной точкой выделения. Векторные символы Qt
и SVG используют одну геометрию. Стрелка не обозначает рассчитанный ток.

На снимке с двумя засечками были не разъединители. В демо-сети их нет.
Просьба заказчика о настоящих выключателях у КТП/трансформаторов **ещё требует
подтверждения изменения электрической модели**: при предыдущем запрете менять
топологию и fingerprint мы не добавляли фиктивные аппараты. Вопрос задан,
не считать этот пункт реализованным. Не начинать новое изменение сети без
решения заказчика и определения размещения/параметров.

Демо-файл содержит сохранённые пользователем перемещения и вид, появившиеся
после прошлой выдачи. Они намеренно сохранены. Его SHA-256:
`898d808dff3dbf13bea28ee9fa6d2e21bcc91bb29c5f1e8aa5d11744f69f75af`.
Ядро, модель, ID, роли портов, эталон и его журнал не изменены. Дословная сверка
эталона — в отчёте и `SchematicVisualFix-Sol-20260831/baseline-check.txt`.

При приёмке начать с этого архива, а не предыдущей копии, выполнить полный
pytest и сверку эталона без `--yes`. Проверить панель под системной тёмной
палитрой, перемещение шины туда/обратно, настоящие Т-присоединения, стрелки
на поворотах/пересечениях и сохранность чисел. Автоматическая native Windows
проверка не заменяет ручную работу мышью.

UI-CRASH-001 и B3-PERF-001 открыты. Нельзя объявлять периодический вылет
исправленным по успешному прогону. `current_turn: Max`, B3 остаётся done;
следующие этапы автоматически не запускаются.
