# Передача Max — ConnectionCleanup после B3

31.08.2026. Ход Sol завершён; `status.json → work_mode.current_turn: Max`.
Новый этап не начат. Это согласованная заказчиком коррекция по 15 ответам и команде «Приступай».

Начинай с `rza-2026-08-31-connection-cleanup.zip`, не со старой копии. Проверенный SHA-256 — в приложенном `.zip.sha256`; хеш самого архива нельзя включить внутрь этого же архива. Он содержит эту передачу в корне и актуальный проект `rza-calc-0.3-safe-hardening/`.

## Входная приёмка на твоей стороне

1. Сверь SHA-256 архива с соседним файлом. Сохрани свою текущую копию; не объединяй её молча с этой.
2. Прочитай `AGENTS.md`, `docs/roadmap/status.json` и `docs/work-verification/connection-cleanup-report.md` целиком.
3. В распакованном проекте выполни полный `python -m pytest -q`. На Windows с PySide6 результат Sol: **1896 passed in 242.65s**, 0 failed/errors/skipped. Без PySide6 твой мини-раннер не заменяет Qt-приёмку.
4. Запусти штатную сверку без `--yes`:

```text
python tools_update_baseline.py --stage ConnectionCleanup-acceptance --reason "Приёмка передачи Sol" --evidence docs/work-verification/connection-cleanup-report.md
```

Дословный результат Sol:

```text
Расхождений с эталоном нет.
```

Эталон и его журнал не обновлены. Native Windows набор: **291 passed in 58.36s**. Демо CLI: **116 OK / 3 FAIL / 0 unresolved**; check возвращает 0, table — 1 из-за тех же трёх нарушений защит.

## Что сделано

- Обычный провод тянется от вывода и фиксируется на допустимую цель без меню типа. В палитре физических линий только ВЛ/КЛ, их свойства вводятся справа после рисования; неизвестная длина/R/X не выдумываются.
- 31 существующая линия демо — 21 ВЛ и 10 КЛ — сохраняет legacy-типы, реальные свойства и ID. Изменены совместное выделение/перенос её штрихов; стрелка не отдельное оборудование. Native-конверсии нет.
- Исправлено повторное подключение legacy-трансформатора после удаления прежней линии: номинал хранится за конкретным выводом. Роли from/to не объявлены автоматически ВН/НН. Неоднозначные/несовместимые напряжения отклоняются.
- На реальном CF1_T найден дополнительный дефект: после переподключения старое имя узла ТТ не следовало за выводом и падал refresh. `editor/legacy_ct.py` сохраняет доказанную привязку за PortId и обновляет только ct_node при явном переподключении, включая сохранение/повторное открытие. Коэффициенты/уставки не меняются; внутренний узел звезды 3W не переносится за физической стороной. Повреждённая привязка отклоняется атомарно. Core/adapter не редактировались.
- Устранены оставшийся явный grabMouse после переноса линии и двойной propertyEdited при декорировании инспектора. Проверено несколько последовательных действий в одном MainWindow, а не только свежие окна.
- Шина рисуется широкой заполненной полосой; белые присоединения остаются на оси. Независимые присоединения разнесены, перенос выбранного не двигает другое из-за совпадения координат. Сохранённая графика демо поправлена явно: 23 трассы, только diagram.routes/revision.
- Удаление локально очищает действительно лишние точки/хвосты, сохраняет используемые подключения/другие страницы и входит в ту же Undo-команду. Временные красные рамки/предпросмотры не остаются после отмены и удаления.
- Явное окончание провода на другом проводе создаёт T; обычное пересечение не соединяет сети. У толстой шины — ортогональный разрыв тонкого провода с реальным экранным зазором. 8 pixel-tests на четырёх масштабах, выделена/не выделена, требуют минимум два чистых фоновых ряда с каждой стороны.

## Что важно не исказить при review

Электрические разделы демо, его fingerprint, core/domain/adapters/topology/io/data, baseline и его журнал неизменны. Новый SHA-256 демо JSON: `6bebd1052a76a17070393bf46ceaa776141d0bc58a3c6884f6e993547bee2215`; отличие от входа — только 23 графические трассы и diagram.revision. Явное пользовательское переподключение, в отличие от простого перемещения рисунка, закономерно меняет электрическую модель; ТТ при этом следует за той же физической стороной.

Перед финальным прогоном зафиксированы 208 файлов исходников/тестов/данных. 10 прежних тестовых файлов изменены под согласованный контракт; AST-аудит перечисляет переименования и изменённые assert. Тесты не объявлены все побайтно неизменными. Прирост общего набора — 188 случаев.

В `docs/work-verification/ConnectionCleanup-Sol-20260831/` — полные логи/XML, 14 финальных PNG настоящего окна, 0 Qt callback errors, исходники проб, freeze и SHA-256 доказательств. История первого 9 failed и следующего визуально недостаточного зазора сохранена отдельно как `intermediate-not-acceptance`; не путать её с последним 1896 passed.

## Границы следующего хода

- 64 дополнительных выключателя не добавлены. AUD-PROT-011 и отдельное разрешение core/штатного расширения baseline остаются открытыми. Не рисовать фиктивные аппараты вместо электрических объектов.
- UI-CRASH-001 и B3-PERF-001 не закрыты этим прогоном. Ручная физическая приёмка мышью не заявляется.
- Ручные смещения подписей не сбрасывались: на старом общем виде остаются 42 отмеченных пересечения. Явная команда «Автоподписи» и её независимые проверки сохранены.
- При ответвлении от существующей физической ВЛ/КЛ запрос физического расстояния сохранён: доля рисунка не равна метрам.
- A3/B4/B9/A11 или иной новый этап — только отдельное задание заказчика.

На ПК заказчика единственная рабочая копия — `C:\Users\shock\OneDrive\Desktop\Клауд\rza-calc-0.3-safe-hardening`. Старая `rza-calc-0.3` не менялась. Выдача лежит отдельно в `C:\Users\shock\Documents\Codex\РЗА — для Claude`, предыдущая выдача восстановима из `РЗА — резерв`. Не создавать там вторую «готовую рабочую» копию.
