# Последняя передача Max — прежняя толщина шин

31.08.2026, Sol. По прямому запросу заказчика «Верни шины, как были по размеру)» возвращён прежний тонкий штрих шин. Это не откат ConnectionCleanup: ВЛ/КЛ, подключение/притягивание, независимые белые точки, очистка после удаления, исправления мыши/инспектора и привязки номинала/ТТ сохранены.

Начинать с `rza-2026-08-31-thin-buses.zip`, а не с предыдущего архива. SHA-256 — в соседнем `.zip.sha256`. Внутри лежат эта передача и проект `rza-calc-0.3-safe-hardening`. `status.json → current_turn: Max`.

Результат Sol: **1896 passed in 258.84s**, 0 failed/errors/skipped. Отдельный native Windows набор: **269 passed in 60.41s**. Два снимка настоящего MainWindow просмотрены, 0 ошибок Qt; геометрия шины в пяти размерах полностью совпала с проверенным SafeDrawing. Восстановлена и прежняя рамка выделения; независимые пересечения остаются без соединения, их экранный зазор учитывает тонкий штрих/масштаб.

Штатная сверка без `--yes`, дословно:

```text
Расхождений с эталоном нет.
```

На входе повторить `python -m pytest -q` и:

```text
python tools_update_baseline.py --stage BusSizeRestore-acceptance --reason "Приёмка возврата толщины шин" --evidence docs/work-verification/bus-size-restore-report.md
```

Эталон и журнал не обновлялись. Код изменения — только `editor/symbols.py` и `gui/editor_scene.py`; пять файлов тестов адаптированы под прежнюю толщину/рамку. Два первоначальных отказа были в одной фикстуре, всё ещё целившейся в край удалённой широкой полосы; исправлен только курсор теста, затем полный прогон повторён. Первоначальные логи сохранены отдельно и не являются финальной приёмкой.

JSON демо побайтно неизменен относительно ConnectionCleanup: SHA-256 `6bebd1052a76a17070393bf46ceaa776141d0bc58a3c6884f6e993547bee2215`. Координаты и трассы, электрические разделы, fingerprint, постоянные ID, роли портов, ядро/адаптер/топология/IO не менялись. Все 208 замороженных файлов проверены перед упаковкой.

Отчёт: `docs/work-verification/bus-size-restore-report.md`; логи, XML, два PNG и проверка целостности — `docs/work-verification/BusSizeRestore-Sol-20260831/`. Предыдущий подробный отчёт ConnectionCleanup сохранён как история; его утверждение о широкой шине заменено этой передачей.

Новые этапы не начаты. AUD-PROT-011/64 дополнительных выключателя, UI-CRASH-001 и B3-PERF-001 остаются открытыми. Ручная физическая приёмка мышью не заявляется.

На ПК заказчика единственный проект — `C:\Users\shock\OneDrive\Desktop\Клауд\rza-calc-0.3-safe-hardening`. Старую `rza-calc-0.3` не менять и вторую рабочую копию не создавать. Архивы выдачи — отдельно в `C:\Users\shock\Documents\Codex\РЗА — для Claude`; предыдущая выдача сохранена в резерве.
