# Читаемость светлой темы — 08.09.2026

Рабочая копия: `C:\Users\shock\OneDrive\Desktop\Клауд\rza-calc-0.3-safe-hardening`.
Исходный коммит: `6c41b764473f066c8b1cfd363d7ba53589b9e089`.

Пользователь сообщил о тёмном фоне и почти невидимом тексте в правом
инспекторе, открываемых окнах и меню правой кнопки. Причина воспроизведена:
общий стиль задавал текст #172033, но не переопределял тёмную системную
палитру для содержимого прокрутки, QTableView, QMenu и выпадающих списков.

При запуске теперь задаётся светлая цветовая схема Qt, полная палитра
активного, неактивного и недоступного состояния и затем общий стиль.
Для прокручиваемых панелей, таблиц, меню, выпадающих списков и подсказок
явно заданы светлый фон и тёмный текст. Выделение и недоступные команды
сохраняют собственные различимые цвета. Изменения ограничены `theme.py`
и применением этой темы в `app.py`.

Проверка до переноса: **191 passed**, 29,27 с. Выполнены существующие
проверки читаемости панели команд, выделения текста, карточек, таблицы,
справочника, открытия последнего проекта и формата запуска. Есть 19 прежних
предупреждений Qt об устаревающем invalidateFilter; ошибок нет.

Визуальная проверка использует настоящий Qt-интерфейс и файл нефтепромысла
с искусственно заданной тёмной палитрой. Рендер выполняется в Qt offscreen;
это не ручная проверка физического экрана или системного файлового диалога
Windows. Пользовательские настройки и файлы схем не сохраняются этим запуском.

Сняты 14 поверхностей до и после исправления: главное окно с выбранным
трансформатором, инспектор, полная карточка и её содержимое, список выбора,
таблица и выделение строк, справочник и карточка марки, настоящее контекстное
меню с выделенным пунктом и меню главного окна. Пример: у содержимого
быстрого инспектора число пикселей тёмного фона #202020 уменьшилось
со 102259 до нуля; фон Window/Base стал #FFFFFF при тексте #172033.
Снимки инспектора, карточки и меню просмотрены вручную; текст читаем.

После переноса проверено побайтовое соответствие двух файлов кода
проверенной версии. Настоящий `ЗАПУСК.bat --smoke-test` открыл окно Qt:
1680×1000, 34 листа, 1016 аппаратов, 1172 маршрута, масштаб 75 %.
Окно наблюдалось через 8,07 с; весь проверочный запуск — 8,48 с.
Ошибок запуска и расчёта нет, код возврата 0. Это одиночный замер,
не оценка ускорения программы.

SHA-256 файла нефтепромысла до и после проверок:
`0f4b04f28f79856c29b01ff9a2356598600fbe738aebe6021d2f67ceb2262715`.
Электрический отпечаток:
`3febeb1ad1fb3141ec3608c0596ba3dadf694a9fd8de2865176baff102da0ff4`.
Геометрия представлений и маршрутов и история команд сохранены.
Визуальный сценарий менял положение обзора только в памяти; файл не записывался.

Артефакты проверки находятся вне рабочей копии:
`C:\Users\shock\Documents\Codex\2026-09-06\ghb\work\light-theme`:
`review-tests.xml`, `proof-before/report.json`, `proof-review/report.json`,
снимки в этих двух папках и `actual-launch.json`/`actual-launch.png`.

Применение темы действует после перезапуска приложения. Геометрия, топология,
данные оборудования, формулы, численный эталон и файл запуска не изменяются.
Исходные пользовательские изменения в `docs/roadmap/status.json` и
`docs/work-verification/main-full-run-report.md` сохраняются вне коммита.

Основание выбора API: [Qt QStyleHints — colorScheme](https://doc.qt.io/qt-6/qstylehints.html#colorScheme-prop)
и [Qt QPalette](https://doc.qt.io/qt-6/qpalette.html). Настройка цветовой схемы
относится к текущему приложению; явно заданные роли палитры сохраняются
при изменении системной темы.
