# Выбор проекта, точечное КЗ и компактная учебная сеть — 09.09.2026

Рабочая копия: `C:/Users/shock/OneDrive/Desktop/Клауд/rza-calc-0.3-safe-hardening`,
ветка `main`. Пользователь согласовал окно выбора, открытие без полного расчёта,
затем точечное КЗ; отдельную учебную сеть 110/35/10/0,4 кВ и один цветной блок.
Переключение выключателей двойным щелчком описано в
[предыдущем отчёте](switch-interaction-2026-09-09.md), коммит `f5b96a5`.

## Изменения

- Светлый выбор проекта, 12 недавних путей, пустой проект и создание своей копии
  учебного шаблона. Автооткрытие последнего выключено по умолчанию.
- Открытие через обычный запуск, явный путь, smoke и меню передаёт `calculate=False`.
  Старый программный API сохраняет значение по умолчанию `calculate=True`.
- Точечный запрос содержит неизменяемый вход, один режим, физический узел/порт
  и выбранные `FaultSpec`. Полный `ProjectResult` им не подменяется.
- Подготовка топологии выбранного режима повторно используется; каждый новый
  узел создаёт свой решатель. Совпадающий запрос использует кэш результата.
- Отдельные строки ошибок для недоступных видов КЗ. BCG показывает максимум
  модулей токов повреждённых фаз и сохраняет оба комплексных фазных тока.
- Запрос выполняется в фоне. Отмена, другой режим или изменённые электрические
  данные не позволяют опубликовать устаревшие числа. Геометрия не входит в
  электрический отпечаток, привязка блока следует за рисунком.

## Выполненные независимые проверки

Стартовое окно: **86 тестов прошли**, 11,52 с, до завершающей интеграции точечного UI.
Проверены ошибки открытия/сохранения/настроек, история и исключительная запись нового
файла. Реальный Qt-проход `QDialog.exec → MainWindow` открыл нефтепромысел:
34 листа, 1016 аппаратов, 1172 трассы; **0 запусков полного ядра, 0 решателей КЗ**.
Исходный JSON, электрический отпечаток и настоящие пользовательские настройки
сохранены. Это функциональная проверка, не сравнительный замер скорости.

Backend: 15 новых тестов и отдельный затронутый набор из 82 проверок прошли.
Они покрывают выбранный режим, недостающие Z0, неправильный несвязанный режим,
точную сторону, отмену, восстановление тех же электрических данных после undo,
устаревшие ответы и просмотр ΔI без полного результата. Наборы пересекаются,
их количества не суммируются.

Дополнительный независимый прогон `reference_probe.py` загрузил сохранённый
исправленный контрольный пример и выполнил 10 точечных запросов:

| Проверка | Результат |
|---|---:|
| Численные случаи против независимого фазного эталона | 28 совпадений |
| Изолированная сеть без ёмкостей | 12 ожидаемых диагностик |
| Наибольшее абсолютное расхождение комплексного тока | 1,637720085286689·10⁻¹⁴ кА |
| Подготовки топологии для MAX/MIN | 2 |
| Создания решателя для десяти точек | 10 |
| Вызовы выбранных четырёх видов КЗ | 40 |
| Полный расчёт проекта/защит | 0 |

Отдельный spy запрещал `Context`, `engine.run/run_input` и старый `solver.at`.
Сохранены контрольные суммы трёх файлов эталона и большого нефтепромысла.

Компактная сеть: **81 тест прошёл**, в том числе 256 доступных исходов КЗ
на 16 секциях шин в четырёх режимах, независимый фазный пример, разделение
вводов, ремонт, привязки маршрутов и сохранение. Числа условного набора и его
границы описаны в [инструкции](../compact-training.md). В метаданных отдельно
проверены названия полей, которые карточки и ядро используют для источников данных.

GUI: перед последними уточнениями разметки пройдены наборы 72 и 95 проверок;
последняя правка подписей полного/точечного расчёта проверена набором **58 passed**.
Наборы пересекаются, их числа не складываются. Дополнительный обзор обнаружил
и устранил два случая обращения вкладки измерений к прежнему полному результату,
включая повтор того же точечного запроса из кэша.

Штатный `ЗАПУСК.bat --smoke-test` завершился с кодом **0**. Внешний наблюдатель
после настоящего `MainWindow.showMaximized` зарегистрировал `visible=true`,
`maximized=true`. Использовался Qt offscreen: это проверка живого окна Qt,
не управление открытым пользовательским экземпляром. Путь настройки в smoke
отключён. Документация, сохранность старых тестовых схем и формат BAT:
**26 passed**. Проверена 71 локальная ссылка; исторические разделы, baseline
и нефтепромысел побайтно сохранены.

## Полный прогон

Завершающая интеграционная проверка нового интерфейса и учебной сети выполняется
отдельно; итог и точный коммит будут записаны здесь после её завершения.
Прежние **3643 passed** относятся к блоку ядра на `1ce00d1`, а не к этому изменению.

## Границы

КЗ в GUI пока металлическое: ABC, BC, AG, BCG с нулевым сопротивлением повреждения.
Доступны существующие электрические узлы и подключённые физические выводы,
для линии — её начало и конец. Внутренняя точка по длине пока не реализована.
Числа точечного запроса не подтверждают правильность действующих уставок.
ΔI ветвей не включает доаварийную нагрузку; ТТ пока идеален.

Компактная схема — отдельный условный набор, не параметры реального объекта.
Старый нефтепромысел не заменяется и не пересобирается. Формулы прежнего полного
расчёта, эталон численных значений и его допуски этим интерфейсным изменением
не меняются. Нет сравнения с отчётами EnergyCS ТКЗ или утверждённого профиля терминала.

Рабочие материалы: `C:/Users/shock/Documents/Codex/2026-09-06/ghb/work/startup-point-query/`
(`chooser-proof.json`, `reference-proof.json`, снимки Qt), отдельные XML в `work/`.
Пользовательская инструкция — [выбор и расчёт КЗ](../point-fault-workflow.md).
