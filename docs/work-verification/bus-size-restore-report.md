# Возврат прежней толщины шин

31.08.2026, Sol. Прямой запрос заказчика: «Верни шины, как были по размеру)».
Новый этап не начат; остальные исправления ConnectionCleanup сохранены.

Входная выдача: `rza-2026-08-31-connection-cleanup.zip`, SHA-256
`e1f0655c8719d5075cf74d4cd3d6f463ec6c10cdafaa191b683b0dadedc82669`.
Перед правкой все 208 файлов кода/тестов/данных совпали с её freeze; независимый review подтвердил отсутствие неожиданных изменений в семи ключевых файлах, включая демо.

## Изменение

- Вместо заполненной полосы восстановлен прежний единственный штрих шины, `stroke_scale=2.25`. Прежние длина, исходные width/height, ось, окончания и роли сохранены. Рамка выделения снова имеет обычный отступ 5 от видимого штриха.
- Убрана зависимость разрыва независимого пересечения от удалённой широкой полосы. Разрыв следует фактическому штриху, косметическому перу, масштабу и рамке выделения; критерий двух чистых фоновых пикселей сохранён.
- Не откатывались независимые белые присоединения, их притягивание, интервалы и перенос, ВЛ/КЛ, исправления мыши/инспектора, очистка лишних точек и сохранение номиналов/ТТ.
- JSON проекта, положения объектов и трасс, электрические разделы, ID и расчётные модули не менялись. Демо SHA-256 по-прежнему `6bebd1052a76a17070393bf46ceaa776141d0bc58a3c6884f6e993547bee2215`, fingerprint `b275e47230495c199e8c4ad0158838af0f3a3c14a2767fc85a98bbe7a1164445`.

## Проверки

Адресный offscreen-набор: **255 passed in 43.89s**. Тот же набор в native Windows: **255 passed in 61.24s**. Он включает пиксельные проверки пересечений на четырёх масштабах с выделением и без, присоединения, движение, поворот, прямую протяжку и независимые проверки подписей.

Настоящий MainWindow: 2 PNG лично просмотрены; 0 ошибок Qt, 5 белых контактов выбранной шины, модель и документ неизменны. Файл проекта не сохранялся.

Независимое сравнение с предыдущим SafeDrawing: геометрия шины полностью совпала через `dataclasses.asdict` на пяти размерах/направлениях. Источник сравнения — резервный ZIP с проверенным SHA-256 `3e1df2e01b14b3910e21658895f29f7e1f83bcb9b90871b8b90d92e8b2202e8b`, а не ручная копия ожидаемых координат.

Первый полный прогон: 1894 passed / 2 failed in 265.54s. Оба отказа — два размера одной проверки притягивания: курсор стоял в жёстко заданной точке y=-19, которая после уменьшения рисунка оказалась дальше края шины плюс 12 пикселей. Исправлена только фикстура: курсор задаётся в 11 пикселях от фактического видимого края. Проверки двух независимых точек, постоянных PortId и точного совпадения preview/commit не менялись. Исходники приложения после первого полного прогона не правились.

Расширенный финальный native Windows набор с этой проверкой: **269 passed in 60.41s**. Штатная сверка без `--yes`: **«Расхождений с эталоном нет.»** Эталон и журнал побайтно сохранены.

Повторный полный итоговый прогон: **1896 passed in 258.84s**, 0 failed/errors/skipped. Финальный native-набор: **269 passed in 60.41s**. Перед запуском заморожены 208 файлов кода/тестов/данных, после запуска хеши совпали. Старые результаты ConnectionCleanup относятся к предыдущей широкой отрисовке и не подменяют новую проверку. Промежуточные логи сохранены отдельно в `intermediate-not-acceptance`, не под именем финальной приёмки.

Исторические отчёты и исходные свидетельства ConnectionCleanup не перезаписаны. Рабочая копия только `C:\Users\shock\OneDrive\Desktop\Клауд\rza-calc-0.3-safe-hardening`; отдельная выдача — `C:\Users\shock\Documents\Codex\РЗА — для Claude\rza-2026-08-31-thin-buses.zip`, SHA-256 рядом. Предыдущая выдача восстановимо перенесена в `РЗА — резерв\2026-08-31-before-bus-size-restore`. Доказательства: `docs/work-verification/BusSizeRestore-Sol-20260831/`. Ручная физическая приёмка мышью и закрытие UI-CRASH-001/B3-PERF-001 не заявляются.
